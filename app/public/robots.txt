# Лаборатория: срезы и собранные игры до релиза (docs/PLAY.md, Gate 4a).
# Ссылка публичная, чтобы открываться с телефона без логина, но в поиске
# этим страницам делать нечего: играют по ссылке, а не находят.
User-agent: *
Disallow: /
