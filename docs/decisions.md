# Журнал решений — проект

Живой документ. Обновляется после каждого согласованного решения.
Формат записи: дата · тема · что решили · почему.

**Два журнала, две области.** Этот — про проект: конвейер, ворота, пороги,
спеки, дистрибуция. Инженерные решения про общий шелл и сборку — в
`app/docs/decisions.md`. Решение, касающееся обоих, записывается здесь, а
оттуда на него ссылаются.

---

## 2026-09-16 — Форк game-template под 30/30

**Решение:** форкаем game-template- под нужды эксперимента.
- Capacitor/Android убирается из дефолтного пути (npm run cap:sync,
  cap add android больше не часть обычного скрипта), остаётся опционально
  для победителя в фазе 2. capacitor.config.ts остаётся в репозитории.
- levelCount фиксируется на 5 (было 9).
- Сигнал: NtfySignalSink заменяется на PostHogSignalSink. Вместо одного
  события more_yes — полный лог: game_open, hub_impression, hub_click,
  teaser_click, first_action, help_open, level_start, level_win,
  level_fail, retry, level_5_complete, rating_submit, comment_submit,
  session_2, return_d1, return_d7. К каждому событию: game_id,
  anonymous_user_id, session_id, timestamp, level, source, version.
- Структура: shell общий в корне репозитория play, в папках отдельных
  игр — только mechanic (выбрано ради простоты поддержки, а не строгого
  соответствия принципу "template is copied, not inherited").

## 2026-09-16 — Экран после 5-го уровня: MoreScreen → попап оценки

**Решение:** MoreScreen (было: да/нет "Ещё?") заменён на попап оценки.
- Звёзды 1–5 — обязательны, кнопка "к уровням" задизейблена, пока не
  выбрана хотя бы одна звезда.
- Поле комментария — опционально.
- Кнопка "к уровням" одновременно сабмит и возврат в LevelSelect;
  уровни можно перепроходить повторно.
- Событие more_yes убрано полностью.
- Событие rating_submit (значение 1–5) летит в PostHog всегда при клике
  на кнопку.
- Событие comment_submit летит в PostHog только если поле комментария
  не пустое в момент клика (сам текст в PostHog не пишется — см. ниже).

## 2026-09-16 — Ворота 3 без more_yes

**Решение:** метрика Ворот 3 (документ "13 — Правила эксперимента 30/30",
раздел 8, вес 2) заменена: вместо доли more_yes среди дошедших до
level_5_complete — доля игроков с оценкой 4–5 звёзд среди дошедших до
level_5_complete. Остальная формула подсчёта рейтинга (раздел 10) не
меняется.

**Открыто:** сам документ "13" в Google Drive пока не обновлён под это
изменение — источник правды по формуле до тех пор физически расходится
с этим логом.

## 2026-09-16 — Сбор комментариев из попапа оценки

**Решение:** текст комментария из попапа оценки отправляется на email
через Web3Forms — без бэкенда, без serverless-функции. Клиент шлёт POST
напрямую на api.web3forms.com с access_key.

**Что летит в письме:** название игры (game_id), anonymous_user_id,
текст комментария.

**Получатель:** stas@hrytsko.com

**Почему так:** самое простое решение из доступных — не нужен ни
Supabase, ни своя serverless-функция с почтовым провайдером. Бесплатный
тир Web3Forms — 250 писем/месяц, для фазы 1 (одна игра в день) с запасом;
в фазе 2 на популярной игре может понадобиться апгрейд — решаем по факту,
не сейчас.

**Что отдельно:** число звёзд (1–5) параллельно летит в PostHog как
событие `rating_submit` — комментарии по почте эту метрику не заменяют.

## 2026-09-16 — PostHog проект подключён

**Project token (client-safe):** `phc_uT9HfgYBxEeHxK2FP9LFJY5ct5ZBtPFD2znLAxPCRFDL`
**Project ID:** 275631
**Region:** EU Cloud

Это Project API Key — write-only, предназначен для клиентских библиотек,
публикация в открытом репозитории безопасна (PostHog сам это подтверждает
в UI: "Safe to use in public apps").

**Важно для реализации PostHogSignalSink:** раз регион EU, capture-эндпоинт
для клиента — `https://eu.i.posthog.com`, а не `us.i.posthog.com` (дефолт
у большинства SDK-примеров). Это должно быть явно указано при инициализации
клиента, иначе события будут либо не долетать, либо писаться не в тот регион.

**Web3Forms:** access_key пока не заведён, в `game.config.ts` — плейсхолдер.

## 2026-09-16 — Форк game-template готов и запушен

**Решение:** форк game-template- под 30/30 сделан, прошёл typecheck/lint/test
(67/67)/build и запушен в этот репозиторий по пути `app/` — общий shell +
`app/games/<slug>/` на каждую игру (games/tap-targets — рабочая заглушка,
подтверждает, что shell + multi-page сборка работают). Полное обоснование
каждого решения — `app/docs/decisions.md`, запись D-011.

**Не сделано в этом проходе:** `npm run e2e` (Playwright) переписан под новую
раскладку и флоу оценки, но не прогнан — негде было поднять headless-браузер.
Прогнать локально перед тем как доверять.

**Открыто:** события `level_fail`, `retry`, `first_action` из документа 13
не реализованы — `MechanicHost` физически не может их сообщить, нужно
осознанное расширение `shell-contract.ts` (не сделано молча). Документ
«13 — Правила эксперимента 30/30» и «Рамка генерации концептов» в Google
Drive всё ещё говорят про 9 уровней и три полосы сложности — ждут отдельной
правки (`app/docs/rules-template.md` уже переписан под 5, см. следующую
запись).

## 2026-09-16 — rules-template.md переписан под 5 уровней

**Решение:** `app/docs/rules-template.md` (чек-лист для написания
`games/<slug>/rules.md`) переписан целиком под `levelCount = 5` без полос
сложности — раньше говорил про 9 уровней и деление 1-3/4-6/7-9, которого в
`LevelSelect` больше нет. Убраны все числовые несоответствия из скелета,
примера и чеклиста; пути в разделе «что делать когда документ готов»
поправлены под `games/<slug>/` вместо одного репозитория на игру.

**Остаётся открытым:** тот же 9-уровневый / трёхполосный язык всё ещё в
Google Drive — документы «13 — Правила эксперимента 30/30» и «Рамка
генерации концептов». Не трогал их в этом проходе.

## 2026-09-16 — render-kit наполнен, без новой зависимости

**Решение:** `app/src/render-kit/` — шесть Phaser-хелперов (punch,
invalidShake, successFlash, fadeCollapse, cameraShake, moveTo), написаны
руками на встроенном Phaser Tween/Camera API, без rex-plugins (исследовал —
156 МБ на диске, нестабильные пути импорта, сами эффекты по 10-20 строк).
`moveTo` добавлен шестым сверх изначальной пятёрки — после чтения полки
30/30 (Google Sheet, 29 концептов) увидел, что большинство двигает объект
из точки в точку, а не только на месте. Обкатано на живом коде —
LevelScene заглушки теперь зовёт `fadeCollapse` вместо своего инлайн-твина.

Полное обоснование — `app/docs/decisions.md`, запись D-012.


---

## 2026-09-19 — Один нормативный шаблон спеки вместо двух

**Решение:** `app/docs/rules-template.md` удалён. Нормативным остаётся
`specs/_TEMPLATE.md`, ремесленная часть переехала в `specs/HOWTO.md` без
собственной нумерации разделов.

- `_TEMPLATE.md` отвечает на вопрос «что должно быть в документе»,
  `HOWTO.md` — «как это написать». Нумерация есть только у первого.
- Из удалённого файла сохранены способы А/Б описания уровней и требование
  называть разницу между каждой соседней парой — оба переехали в §6 шаблона,
  на них ссылается шаг 5 сборки.
- Ссылки в `app/CLAUDE.md` исправлены: раньше шаг 1 отправлял агента читать
  «§4 таблица решений, §5 победа, §6 поражение» — нумерацию удалённого
  шаблона. В реальных спеках по этим номерам лежат состояние, ввод и контент.

**Почему:** два документа с собственной нумерацией разделов — это гарантия,
что ссылка вида «§4» рано или поздно промахнётся. Здесь она промахивалась с
самого начала, и заметить это можно было только прочитав оба файла подряд.

Предыдущие записи про `rules-template.md` (16 сентября) остаются как история:
файл существовал и переписывался под 5 уровней до того, как был слит.

---

## 2026-09-19 — Закрыт разрыв first_action / level_fail / retry

**Решение:** общий контракт `Shell ↔ Mechanic` расширен двумя событиями:
`onFirstAction()` и `onFail(reason)`.

- Первый осмысленный игровой ввод теперь даёт `first_action` в PostHog.
- Проигрыш механики даёт `level_fail` с номером уровня и стабильной причиной.
- После проигрыша общий shell показывает fail-popup; кнопка повтора сама
  логирует `retry` и перезапускает тот же уровень.
- `retry` не добавлялся в контракт механики: это действие оболочки.
- Поздние события от уже уничтоженной сессии игнорируются.

**Почему:** первая запланированная игра уже имеет условие поражения
(`pocket_overflow`), а старый контракт умел сообщать только победу и выход.
Без этой правки аналитика эксперимента теряла бы начало реальной игры,
поражения и повторные попытки.

---

## 2026-09-19 — Шаг RELEASE: `release.mjs`, две разные ссылки

**Решение:** публикация игры на хаб — одна команда `node release.mjs <slug>`.

- `/g/<slug>/` — играбельная сборка, `/<slug>/` — страница с описанием и
  цифрами. Две разные вещи по двум разным адресам, без коллизии.
- `g/assets/` наполняется **дополнительно** и никогда не чистится: имена
  файлов хешированные, и HTML уже опубликованной игры ссылается на свой хеш.
  Удаление «старых» ассетов сломало бы вышедшую игру задним числом.
- Sourcemaps не копируются — это 12 из 13 мегабайт сборки, а репозиторий
  хостится как есть.
- Скрипт отказывается публиковать, если слага нет в `games.json`. Не
  создаёт запись сам.
- Собственных ворот качества у скрипта нет, кроме typecheck и сборки:
  ворота — это CI.

**Почему:** до этого у собранной игры не было пути в продакшен вообще.
Сборка лежала в `app/dist/games/<slug>/`, страница-описание — в
`<корень>/<slug>/`, и ничто не копировало одно в другое; `links.play`
проставлялся бы руками на адрес, которого не существует.

Отказ при незнакомом слаге — не перестраховка: слаг спеки и слаг хаба уже
разошлись однажды (`box-arrives` против `incoming-box`). Молчаливая
публикация под вторым именем развела бы их окончательно.

---

## 2026-09-19 — PLAY определён как production pipeline прототипов

**Решение:** добавлен канонический high-level документ `docs/PLAY.md`.

PLAY определён не как experimentation platform, а как лёгкий повторяемый
конвейер производства игровых прототипов:

`IDEA → SPEC → GAME → CHECK → HUMAN REVIEW → RELEASE → DISTRIBUTION → FEEDBACK`.

Ключевое разделение:

- **framework** — общая переиспользуемая техническая база;
- **production flow** — одинаковый путь производства каждой новой игры.

Идея не обязана становиться отдельным файлом: автор может прислать её
свободным текстом или голосом, AI нормализует её, а первым обязательным
артефактом остаётся `specs/<NN>-<slug>.md`.

Роли также зафиксированы явно: человек придумывает и утверждает; AI
структурирует, специфицирует, реализует и готовит distribution copy;
автоматика проверяет/собирает/публикует; framework обеспечивает единый
стандарт; игроки дают фактическую реакцию.

**Почему:** задача PLAY — не строить монструозную лабораторию экспериментов, а
сделать производство разных маленьких игр быстрым, единообразным и
повторяемым.

---

## 2026-09-19 — Один слаг на игру; название и питч живут только в спеке

**Решение:** убраны три дубля сразу, потому что они держались друг за друга.

- **Слаг один.** День 1 был `box-arrives` в `specs/` и `incoming-box` в
  `games.json`. Оставлен слаг спеки, `box-arrives`: остальные двадцать девять
  игр выходят под слагами спек, и держать одно исключение дороже, чем
  переименовать. Ничего не опубликовано, так что цена переименования нулевая.
- **Расписание одно.** `distribution/schedule.json` удалён. День → слаг для
  всех тридцати дней лежит в `games.json`. Провизорность порядка видна по
  статусу `planned`, а не по тому, в каком файле лежит строка.
- **Название и питч одни.** Из `games.json` убраны `title`, `sourceTitle`,
  `pitch` и блок `concept`; из `distribution/posts.json` — `title` и `today`.
  И хаб, и генератор постов читают их из шапки спеки через общий
  `specs/front-matter.mjs`. В `posts.json` осталось то, что действительно
  пишется отдельно: интрига «завтра» и десять постов прогрева.

**Почему:** `concept` в `games.json` не читал вообще никто — это была копия
шапки спеки, которая молча устаревала. Питч при этом жил в трёх местах и уже
разошёлся: на карточке хаба стояла одна формулировка, в спеке другая, в
постах третья. Пока источник один, «поправить название» — это одна правка, а
не три и попытка вспомнить, где ещё.

Расхождение слагов стоило бы дороже всего: тексты постов лежат под слагом
спеки, ссылка строилась по слагу хаба, и день без постов обнаружился бы
вечером своего дня. Теперь `specs/validate.mjs` падает в CI, если в
`games.json` появится слаг, которого нет в `specs/`, или один слаг встанет на
два дня.

**Проверка:** CSV после переключения сошёлся байт в байт, кроме одной ссылки —
день 1 перестал уезжать на несуществующий `/incoming-box/`.

---

## 2026-09-19 — Этап IDEA начинается с концепта 36, а не задним числом

**Решение:** заведены `docs/templates/idea.md`, папка `ideas/` и
`ideas/validate.mjs` (шапка, восемь непустых разделов, слаг). Папка пока
пуста, и это не недоделка.

Тридцать существующих спек задним числом в идеи не переписываются. Идея,
собранная из готовой спеки, повторяет уже принятые решения, а её
kill-критерий оказывается подогнан под то, что написали позже, — то есть
делает ровно то, против чего этап и придуман.

**Почему шапка идеи короче шапки спеки:** в ней нет `pitch_ru`, `pitch_en` и
`score`. Питч — это формулировка, которая рождается, когда игра уже описана;
оценка по шести критериям — сравнение с другими концептами. Требовать их на
входе значит получить выдуманное значение вместо честно отсутствующего.

---

## 2026-09-19 — Файл идеи необязателен: `ideas/` приведён к `docs/PLAY.md`

**Решение:** `ideas/` остаётся, но перестаёт быть воротами.

`docs/PLAY.md` — канонический документ — говорит про шаг IDEA прямо:
обязательного файла нет, идея приходит сообщением, голосом или наброском, а
первый обязательный артефакт — спека. Заведённая часом раньше папка `ideas/`
с проверкой в CI это правило нарушала, хотя и не блокировала пустой случай.

Что изменено:

- `docs/templates/idea.md` описан как форма brief, а не как обязательный
  документ. Восемь вопросов одинаковы для файла и для разговора.
- `ideas/validate.mjs` проверяет форму только тех идей, которые записаны
  файлом. Пустая папка — не ошибка, и это теперь сказано в самом скрипте, а
  не только в README.
- В `docs/pipeline.md` у шага IDEA больше нет ворот.

**Что осталось аргументом за файл:** kill-критерий. Он работает, только если
датирован до первых цифр; записанный после — подогнан под них. Это
единственная причина заводить файл, и она названа единственной, а не
расширена до «заведи файл на всякий случай».

**Заодно:** в `docs/pipeline.md` добавлен раздел «Расхождения с PLAY.md».
Там названы ещё три: разные имена шагов, отсутствие HUMAN REVIEW отдельным
этапом (фактически он есть — `release.mjs` запускается руками) и то, что
DATA/RESULT с полем `verdict` строже, чем мягкий FEEDBACK в PLAY.md.
Противоречия нет, пока вердикт остаётся решением человека.

**Почему не наоборот.** Два документа об одном конвейере — это тот же дубль,
с которым разбирались весь день. Правило выбрано одно и записано в обоих:
общая картина и ответственность — `PLAY.md`, артефакты и ворота —
`pipeline.md`, и при расхождении прав `PLAY.md`.

---

## 2026-09-20 — Конвейер согласован: четверо ворот, три из них человека

**Решение:** флоу зафиксирован целиком, структура репозитория приведена к нему.

```
TRENDS → IDEA → [gate 1] → SPEC → [gate 2] → BUILD → [gate 3: CI]
       → [gate 4: человек играет] → MEDIA → RELEASE → DISTRIBUTION → VERDICT
```

Что появилось:

- `trends/` — датированные наблюдения по рынку. Не этап и не артефакт, ворот
  нет. Но балл `market` на Gate 1 опирается на эту папку: если балл 5, а в
  `trends/` об этом ничего, балл выдуман.
- `ideas/active/` и `ideas/rejected/`. **Файл идеи снова обязателен** — см.
  отдельную запись ниже про разворот. Папка и поле `gate1` — одно состояние,
  записанное дважды, валидатор падает при расхождении.
- Gate 1: шесть критериев по 1–5 в шапке идеи, порог сумма ≥ 24 **и**
  `producibility` ≥ 3. Баллы ставит AI, решение — человека.
- Gate 2: `review: pending | rework | approved` в шапке спеки.
- Gate 4: `app/games/<slug>/review.md`, и `release.mjs` отказывается
  публиковать игру, у которой там не `approved`.
- `app/games/<slug>/media/` — обложка, баннер, кадр, ролик, тексты игры.
- `data/` и `results/` с форматом и правилом вердикта.

**Почему порог производимости отдельный и не компенсируется суммой.** Игра
делается за один вечер. «Гениально, но три дня работы» в тридцатидневном
эксперименте отнимает три прототипа, то есть стоит дороже, чем даёт, при
любом балле по остальным пяти критериям.

**Почему порог 24 назначен, а не выведен.** Все тридцать концептов 1–35 уже
прошли отбор до того, как балл появился: разброс у них 20–28 при медиане 26,
снизу ничего плохого просто нет. Порог, посчитанный по выжившим, пропускал бы
всё.

**Почему ворота записываются файлами, а не держатся в голове.** Три из
четырёх — решение человека, и ровно они пропускаются первыми в вечер, когда
некогда. Поле в шапке и отказ `release.mjs` превращают намерение в ворота.

---

## 2026-09-20 — Вердикт: доля дошедших, минимум 30 прохождений, окно месяц

**Решение:** каждому прототипу месяц с даты релиза. Вердикт по правилу:

| Условие | Вердикт |
|---|---|
| 30+ прохождений и доля от открытий ≥ 25% | PROMOTE |
| 30+ прохождений и доля ≤ 10% | KILL |
| 30+ прохождений, доля между | решает человек по повторам и возвратам |
| меньше 30 прохождений | INCONCLUSIVE |

**Почему доля, а не абсолютное число.** Игра первого октября выходит на
аудиторию в тридцать человек, игра тридцатого — на выросшую за месяц.
Абсолютные числа между ними несравнимы, и порог по ним систематически
награждал бы поздние игры за календарь.

**Почему 30 — минимум.** При 30 прохождениях из примерно 120 открытий
доверительный интервал на долю около ±8 процентных пунктов: 25% отличимо от
10%. При 10 прохождениях интервал ±12, то есть шире самой разницы, которую им
собираются мерить.

**Почему рейтинг последним и только от 30 оценок.** Оценку ставят те, кто
дошёл до конца, — то есть те, кому уже понравилось. Отвалившиеся уходят
молча и в среднюю не попадают. Это та же выживаемость, из-за которой нельзя
было вывести порог Gate 1 из существующих баллов. Ниже 30 оценок средняя не
показывается вообще, чтобы ею не соблазняться.

**Добавлен сигнал `returnedLater`** — вернулся ли игрок в другой день сыграть
в ту же игру. На маленькой выборке честнее рейтинга: возврат ничего не стоит
подделать самому себе.

**Следствие по календарю:** вердикт по первой игре — 1 ноября, по
тридцатой — 30 ноября. Эксперимент закрывается концом ноября, а не октября.

---

## 2026-09-20 — Разворот: файл идеи снова обязателен

**Решение:** отменена вчерашняя запись «файл идеи необязателен».

Вчера `docs/PLAY.md` требовал не заводить бюрократию на входе, и ворота с
идеи были сняты. Сегодня автор решил иначе: работа над прототипом начинается
с файла, и Gate 1 — настоящие ворота с порогом и подписью.

`PLAY.md` переписан под это же, так что противоречия снова нет. Запись
оставлена как есть: разворот за сутки — нормальная часть проектирования, а
затёртая история решений хуже, чем история с разворотом.

**Что из вчерашнего аргумента выжило:** kill-критерий пишется на входе, до
первых цифр. Вчера это была единственная причина заводить файл; сегодня
причин больше, но эта осталась главной.

---

## 2026-09-20 — Доска конвейера: `pipeline.mjs`

**Решение:** статус проекта показывает одна команда, а не таблица.

```sh
node pipeline.mjs            # доска целиком
node pipeline.mjs --waiting  # только то, что ждёт решения человека
node pipeline.mjs --json     # то же машиночитаемо
```

**Ни одного поля `status` нигде не появилось.** Стадия — функция от файлов:
есть идея, есть спека и что стоит в её `review`, есть `game.config.ts`, есть
`review.md` с `approved`, заполнен ли `links.play`, снята ли выгрузка, есть ли
вердикт. Поэтому доска не может устареть — в ней нечего забыть проставить.

**Главная колонка — «Ждёт тебя».** Три из четырёх ворот — решение человека, и
ровно они пропускаются первыми вечером, когда некогда. Доска, которая
показывает всё, отвечает на вопрос «как дела»; `--waiting` отвечает на
вопрос «за что браться», а он задаётся чаще.

**Почему CLI, а не страница.** Терминал уже открыт, страницу надо открыть и
обновить. Плюс доска показывает внутренние состояния — какая игра на
доработке, какая идея отклонена, — а `play.hrytsko.com` отдаёт корень
репозитория как есть, так что отдельная страница означала бы публиковать
внутреннюю кухню. `--json` оставлен на случай, если страница когда-нибудь
понадобится: данные для неё уже есть.

**Проверено прогоном всех стадий:** идея без оценки → оценена → одобрена →
спека pending → gate 2 → собрана → gate 4 → принята → опубликована (с
обратным отсчётом месяца) → цифры → вердикт, плюс случай «прохождений 11 из
30», где вердикт откладывается. Тестовое состояние откачено.

---

## 2026-09-20 — Доска страницей: `dashboard/`, отдельный проект Vercel

**Решение:** та же доска, что и в терминале, отдаётся страницей из папки
`dashboard/` — отдельный статический проект Vercel с Root Directory
`dashboard`, без сборки.

**Данные приезжают с коммитом.** `node pipeline.mjs --write` пишет
`dashboard/status.json`, он лежит в репозитории. Никакого API и никакой
сборки на деплое: доска ровно настолько свежа, насколько свеж последний пуш,
и это не компромисс, а точность — состояние системы **и есть** содержимое
репозитория.

**`node pipeline.mjs --check` в CI.** Доска, которая показывает вчерашнее
состояние и выглядит при этом свежей, — худший вид поломки: ей верят.
Проверка сверяет только то, что зависит от файлов; дата и обратный отсчёт до
старта меняются сами, и сверять их значило бы ронять CI каждую ночь. Страница
дополнительно подсвечивает снимок старше трёх дней, если экран висит на
старом деплое.

**Цвет посчитан, а не подобран.** Воронка — порядковая рампа в одной
тональности, выведена из акцента хаба и прогнана валидатором в обеих модах:
монотонная светлота, шаги ≥0.06, светлый конец держит 2:1 к поверхности.
Тёмная мода — свои шаги под тёмный фон, а не автоматический инверс.

**Воронка считает накопленно** — «дошли хотя бы до стадии», а не долю от
целого. Доля от целого показывала бы ранние стадии как достижение: тридцать
концептов на стадии «спека» выглядели бы заполненной полосой.

**Единственная крупная цифра на странице — «ждёт тебя».** Всё остальное
отвечает на вопрос «как дела», а этот блок — на «за что браться», и он
задаётся чаще.

**Доступ закрывается на стороне Vercel** (Deployment Protection): доска
показывает, какие идеи отклонены, что на доработке и какие игры выйдут в
какие дни. Отдельно отмечено, что корень репозитория отдаётся хабом как есть,
так что `play.hrytsko.com/dashboard/` тоже откроется — репозиторий публичный,
секретом это и не было.

**Проверено рендером** в светлой и тёмной моде и на ширине телефона: ошибок
консоли нет, горизонтальной прокрутки нет. Первый прогон её дал — колонка
«Ждёт» не влезала, — и это чинилось не «на глаз», а после скриншота.

---

## 2026-09-20 — Правило: доска не отстаёт от репозитория

**Решение:** любое изменение, которое двигает что-то по конвейеру, попадает на
доску **в том же коммите**. Правило записано первым в `CLAUDE.md` и в
`docs/PLAY.md`.

Движением считается: заведена, оценена, одобрена или отклонена идея; файл
переехал между `ideas/active/` и `ideas/rejected/`; появилась или принята
спека; собрана игра или подписан `review.md`; игра опубликована; переставлено
расписание; снята выгрузка; вынесен вердикт; добавлено наблюдение в `trends/`.

**Почему правило, а не пожелание.** Доска, которая показывает вчерашнее
состояние и выглядит при этом свежей, — худший способ сломаться: ей верят и по
ней принимают решения. Пустая доска честнее отставшей.

**Правило не держится на памяти — три слоя, и ни один не про дисциплину:**

1. Стадии нигде не хранятся, они считаются из файлов. Поля `status`, которое
   можно забыть переставить, не существует; забыть можно только пересчитать
   снимок.
2. `.githooks/pre-commit` пересчитывает `dashboard/status.json` и сам
   добавляет его в коммит. Включается один раз:
   `git config core.hooksPath .githooks`. Если `node` недоступен — хук молча
   выходит, чтобы не ломать коммит там, где его нечем проверить.
3. `node pipeline.mjs --check` в CI ловит то, что прошло мимо хука.

**Проверено:** изменён `gate1` в идее, сделан коммит без единого ручного
касания `status.json` — хук подтянул обновлённый снимок в тот же коммит.
Пробный коммит откачен, `gate1` возвращён в `pending`: это решение человека.

**Заодно на доску добавлены наблюдения из `trends/`** — счётчик и сколько дней
назад было последнее. Формально это не стадия конвейера, но это то, что в
проекте меняется чаще всего, и без него неделя работы с трендами выглядела бы
на доске неделей простоя. Правило говорит «любое движение», значит и это.

**Заодно заведён `CLAUDE.md`** в корне — три правила, которые держат систему:
доска не отстаёт, один источник правды на сущность, ворота не пропускаются.
Раньше они были размазаны по `docs/`; теперь лежат там, куда смотрят первым.

---

## 2026-09-20 — Правило №1 переведено с дисциплины на устройство

**Решение:** снимок доски больше не коммитится. `dashboard/status.json` — в
`.gitignore`, его собирает Vercel на каждом деплое командой
`node pipeline.mjs --write`. Pre-commit хук и `pipeline.mjs --check` удалены.

**Что было не так.** Утром правило «обновляй доску» держалось на трёх опорах:
привычке, хуке и проверке в CI. Хук при этом включался вручную
(`git config core.hooksPath .githooks`) — то есть на новой машине его надо
было **вспомнить**, а вся суть правила была в том, чтобы вспоминать не
приходилось. Опора, которую надо не забыть включить, — это та же память, но с
лишним файлом.

**Как теперь.** Забывать нечего, потому что нечего делать:

- Стадии нигде не хранятся, считаются из файлов. Двигая конвейер, ты уже
  обновил доску.
- Снимка нет в репозитории. Обновить файл, которого не существует,
  невозможно.
- Vercel собирает его на каждом пуше. Сломанный генератор роняет деплой, а не
  тихо отдаёт вчерашнее.

**Root Directory у проекта доски — корень, а не `dashboard/`.** Генератору
нужен весь репозиторий: он читает `specs/`, `ideas/`, `games.json`,
`app/games/`. Build Command `node pipeline.mjs --write`, Output Directory
`dashboard`. Настраивается в интерфейсе Vercel, а не файлом: `vercel.json` в
корне читали бы оба проекта, и хаб начал бы отдавать доску вместо себя.
`dashboard/vercel.json` поэтому удалён.

**Вторая утечка свежести, которую это не чинило.** «Месяц вышел» наступает без
единого коммита. Доска, собранная при последнем пуше, показывала бы «идёт
28/30 дн.» ещё неделю после того, как месяц истёк, — и никакой хук тут не
помогает, потому что коммита не было. Поэтому всё, что зависит от даты,
страница пересчитывает **в браузере** при каждом рендере: обратный отсчёт до
старта, «идёт N/30 дн.», «месяц вышел», «снять цифры». Пороги приезжают в
данных (`rules` в payload), чтобы остаться записанными в одном месте —
`pipeline.mjs`.

Итого: пуш обновляет то, что зависит от файлов; открытая вкладка — то, что
зависит от даты. Ручного шага нет ни там, ни там.

**Проверено:** в снимок подложены `releasedAt: 2026-08-01`, `note: "идёт 5/30
дн."` и пустой список ожидающих. Страница пересчитала на «месяц вышел», добавила
«снять цифры» в список и подняла счётчик с нуля до двух. Заодно перебила
подложенный `daysToStart: 999` на верные 11.

**Что осталось требовать внимания, и это сказано в правиле прямо:** новый
артефакт или новая стадия в конвейере должны появиться в `pipeline.mjs`. Не
появятся — доска молча соврёт о полноте: покажет всё, кроме нового, и по ней
не будет видно, что чего-то не хватает.

---

## 2026-09-20 — Часы прототипа идут от его публикации, не от старта проекта

**Решение:** с доски убраны общая дата старта и обратный отсчёт до неё. Вместо
них — дата публикации каждой игры и окно, отсчитанное от неё.

Было: `2026-09-20 · старт 2026-10-01, через 11 дн.` в шапке и «30 дней с 1
октября» подписью к таблице. Стало: колонка «Вышла» с датой публикации и
колонка «Состояние» — `идёт 15/30 дн.`, `месяц вышел`, потом цифры и вердикт.

**Почему так правильнее.** Общая дата старта ничего не говорит о конкретном
прототипе. Игра, вышедшая двадцатого, измеряется не «двадцатым днём
эксперимента», а своими тридцатью днями с двадцатого числа. Дата старта живёт
в `games.json` и нужна расписанию постов — на доске ей делать нечего.

Это же снимает лишнюю связь: доска перестала зависеть от `project.startDate`,
и перенос старта её больше не ломает.

**Фон чёрный, светлой темы нет.** Доска рассчитана висеть на экране, а не
подстраиваться под настройку системы. Порядковая рампа воронки перепроверена
валидатором против новой поверхности `#0e0e0d`: ближний к фону шаг держит
2.49:1, шаги ≥0.06, одна тональность. Текст на чёрном: 18.7:1 основной,
8.8:1 приглушённый, 4.9:1 самый тихий, 10.1:1 «ждёт тебя».

**Проверено рендером** на подложенном состоянии середины эксперимента —
вердикт, цифры, две опубликованные с разными датами, принятая, собранная и
спека на приёмке. Первый прогон дал наезд заголовков «ОПУБЛИКОВАНА» и
«СОСТОЯНИЕ»; колонка переименована в «Вышла». Горизонтальной прокрутки нет ни
на десктопе, ни на телефоне, ошибок консоли нет.

---

## 2026-09-20 — Снимок доски возвращён в репозиторий

**Решение:** `dashboard/status.json` снова коммитится. Проект доски на Vercel —
чистая статика: Root Directory `dashboard`, Build Command пустой.

**Что было сломано.** Часом раньше снимок убрали в `.gitignore` и переложили
его сборку на Vercel: Root Directory корень, Build Command
`node pipeline.mjs --write`, Output Directory `dashboard`. Страница на
задеплоенной доске получила 404 на `status.json` и показала сообщение об
ошибке вместо состояния проекта.

**Почему так вышло.** Сборочная команда, которая читает файлы выше Root
Directory, зависит от отдельного переключателя Vercel. Я предложил
конфигурацию, которую не мог проверить из этой среды, и сделал работу
страницы зависимой от настройки в интерфейсе. Хуже того, поломка выглядит как
пустая страница, а не как красный деплой: тихий отказ вместо громкого.

**Почему возврат — не откат к прежней проблеме.** Утром отказались от
коммита снимка потому, что его обновление держалось на pre-commit хуке,
который надо было **вспомнить включить** на каждой машине. Но хук никогда не
был обязательной частью: `node pipeline.mjs --check` в CI гоняется на каждом
пуше и ничего не требует включать. Именно он и есть механизм. Хук был лишним
слоем, и правильным выводом было убрать хук, а не файл.

Итого сейчас:

- снимок в репозитории → страница работает на чистой статике, при любой
  настройке хостинга;
- `--check` в CI → забыть обновить нельзя, пуш краснеет;
- дата считается в браузере → висящая вкладка не устаревает и без деплоя.

**Проверено** копией папки `dashboard/` в отдельный каталог и её раздачей как
есть — ровно то, что сделает Vercel: ни одного 404, ни одной ошибки консоли,
доска отрисована полностью. И отдельно — что `--check` действительно падает:
изменён `gate1` без пересчёта снимка, проверка вернула ошибку и код 1.

**Урок, который стоит держать:** конфигурация, которую нельзя проверить, — это
не решение, а обещание. Предпочитать вариант, работающий без настроек, даже
если он выглядит менее элегантно.

---

## 2026-09-20 — «Ждёт решения» теперь с адресом

**Решение:** каждый пункт списка ожидающих несёт три вещи: ссылку на файл, что
в нём прочитать и куда записать ответ. Плюс команду, когда действие — это
команда.

Было: `arrow-flip · #36 · gate 1 — твоё решение`. Стало то же плюс ссылка на
`ideas/active/36-arrow-flip.md`, «прочитать: идею целиком и раздел „Оценка“ в
конце (25/30, порог 24)» и «записать: в шапке `gate1: approved | rejected` и
`gate1_date`; при rejected — файл переезжает в `ideas/rejected/` с одной
строкой почему».

**Почему это не украшательство.** Пункт без адреса начинается с вопроса «а где
это лежит» — и поэтому откладывается. Три из четырёх ворот держатся на
человеке, и именно они теряются первыми; напоминание без адреса работает
против себя.

**Адрес репозитория берётся из `git remote`,** а не вписан в код: переедет
репозиторий — переедут ссылки. Нет git — просто не будет ссылок, доска от
этого не ломается.

**Ярлык исполнителя.** У пунктов, которые ждут не человека, стоит `AI`,
`скрипт` или `ты запускаешь`. Подпись под крупной цифрой поэтому «ждёт
действия», а не «ждёт твоего решения»: из шести пунктов в тестовом состоянии
два были не решением человека, и утверждать обратное значит подрывать доверие
к остальным четырём.

**Попутно найден настоящий баг.** `overlayToday()` на странице пересобирает
список ожидающих из строк — и **терял `action`**: ни ссылки, ни подсказок не
отрисовывалось вообще. Нашлось не чтением кода, а проверкой отрисованного
DOM: селектор `.path` не находился. Заодно чинится и то, что строка, ставшая
ожидающей уже в браузере («месяц вышел»), теперь собирает себе действие сама
— в снимке его не было и быть не могло.

**Проверено** на подложенном состоянии с шестью ожидающими разных типов:
вердикт, снять цифры, релиз, gate 4, gate 2, gate 1. Ссылка ведёт на
`github.com/stasHrytsko/play/blob/main/...`, на телефоне подписи переносятся
в одну колонку, горизонтальной прокрутки нет.

---

## 2026-09-20 — Тридцать спек без Gate 2 перестали быть невидимыми

**Найдено вопросом:** «а что с расписанием, они ведь тоже на мне?» Да. И доска
об этом молчала.

`pipeline.mjs` разбирал `review` в шапке спеки на три случая — `approved`,
`rework`, `pending` — и на отсутствие поля **не делал ничего**: ни ожидания,
ни пометки. У всех тридцати спек 1–35 поля нет, они писались до появления
Gate 2. На доске они выглядели ровно так же, как пройденные ворота: пустая
клетка в колонке «Состояние» и ни слова в списке ожидающих.

Это ровно тот отказ, про который написано в `CLAUDE.md`: **доска врёт
умолчанием.** По пустой клетке нельзя отличить «сделано» от «не дошли руки».

**Исправлено двумя движениями:**

- В таблице у каждой такой спеки теперь стоит `без gate 2`. Тридцать
  одинаковых пометок — это не шум, это форма самой проблемы, и она видна.
- В «ждёт тебя» попадает **только ближайшая по расписанию** — сейчас день 1,
  `box-arrives`, с подписью «следующая по расписанию из 30 без gate 2».

**Почему очередь, а не тридцать пунктов.** Gate 2 нужен каждой спеке, но игры
делаются по одной: ворота имеет смысл проходить перед сборкой, а не пачкой за
месяц вперёд. Тридцать одинаковых строк утопили бы Gate 1, релизы и вердикты —
а список, в который перестают смотреть, не лучше пустого.

**Правило на будущее, записано в `CLAUDE.md`:** ветка «значения нет» — это не
«нечего делать». Чаще всего это «никто ещё не занимался», и её надо
обрабатывать явно. Пустых клеток на доске быть не должно.

---

## 2026-09-20 — Тридцать спек доведены до шаблона целиком

**Решение:** все тридцать спек проходят `specs/validate.mjs` без единого
предупреждения. Было 65.

Что было недоделано после сентябрьской миграции: §11 оставался неотвеченным
чеклистом во всех тридцати, в §1 у двадцати двух стоял TODO вместо обоснования
kill-критерия, у девяти не было разбивки `score`, у четырёх — `family`.

**Структурная правка вместо тридцати копий.** Четыре из семи вопросов §11 —
поведение общего шелла, одинаковое для всех игр. Они переехали в §9.3 шаблона,
отвеченные **из кода**: поворот экрана перекладывает раскладку через
`Phaser.Scale.RESIZE` без потери состояния; возврат в уровень начинает его
заново, потому что прогресс хранит только номера пройденных уровней; попап
показывается всегда, потому что у `onComplete()` один путь. «Нет отмены хода»
стало явным умолчанием §9.2. В §11 осталось только то, что зависит от
механики, а валидатор теперь считает неотвеченные пункты.

**Ответы вскрыли дыру одной и той же формы — что делать, когда легального хода
нет вообще.** Правила описывали, что происходит при недопустимом действии, но
не при отсутствии любых действий:

- `09-sprout` — ветку можно запереть, когда шаги ещё есть;
- `10-flows` — поворачиваемых сегментов может не остаться, а вода без хода не
  идёт;
- `11-burning-land` — текущая фигура может никуда не влезать, а огонь
  распространяется только после постановки;
- `02-two-moves-later` и `07-stalled` — неподвижная парковка не даёт таймеру
  тикнуть;
- `26-cable-bet` — кабель может кончиться так, что ни один маршрут не
  подтверждается.

Каждый случай — уровень, зависающий без исхода. Теперь во всех шести спека
говорит, что позиция завершается немедленно.

**Два ответа оказались самим концептом в одной строке.** В `08-turnaround`
повторный свайп ничего не делает, потому что успешный ход уже повернул стрелку
от него. В `33-ripeness` **проиграть нельзя вообще**: давление — это разрыв
между `score` и `optimalScore`, а не риск поражения, и теперь это написано там,
где читатель об это споткнётся.

**`family` сведено к одной форме:** строчными, через дефис. Было три разных
стиля в одном наборе — `Sort`, `Cut / Match-2`, `route puzzle / grid
navigation.` — по таким значениям нельзя сгруппировать, а группировать по
семейству придётся при выборе следующих идей. Словарь остался открытым,
проверяется только форма.

**Все тридцать получили `review: pending`** — то есть вошли в Gate 2 по-честному.
Доска при этом показывает ожидающей только ближайшую по расписанию: тридцать
одинаковых пунктов утопили бы остальное.

### Что показали оценки

После заполнения девяти недостающих разбивок видно всё распределение: сумма от
20 до 28. **Десять концептов из тридцати не взяли бы порог Gate 1** (сумма ≥ 24
и `producibility` ≥ 3). Пять из них были оценены до этого прохода, пять —
сейчас, так что это не перекос одного оценщика.

Ниже порога: `25-jammed` (20), `26-cable-bet` (20), `24-excavation` (21),
`32-trail` (22), `33-ripeness` (22), `15-ribbon` (23), `20-collapse-wire` (23),
`21-collapsed` (23), `29-by-touch` (23), `34-arrow` (23).

Сегодня это ничего не блокирует: спеки 1–35 писались до появления Gate 1 и
остаются дедушкиными. Но число — это информация, и решение по нему за автором:
либо порог 24 верен и треть первых тридцати идей его не проходит, либо порог
завышен относительно реального распределения. Порог менять не стали: ворота,
которые никого не отсеивают, не ворота.

**Отдельный практический сигнал — производимость.** Четыре концепта имеют
`producibility: 2`: `21-collapsed`, `24-excavation`, `34-arrow` и дорогие по
авторским уровням. Это самое полезное для расписания: дешёвые концепты стоит
ставить в начало, пока фабрика ещё обкатывается.

---

## 2026-09-20 — Рефактор: два документа удалены, три генератора под одной проверкой

**Решение:** убрано то, что пережило свою правду, и всё приведено к одной
логике. Проверено не на глаз: скрипт прошёл по всем markdown-файлам и сверил
каждую ссылку на путь с тем, что реально лежит в репозитории.

### Удалено

**`app/docs/architecture.md` (498 строк).** Описывал исходный шаблон до форка:
`src/mechanic/`, `src/game.config.ts`, `scripts/new-game.ts`,
`scripts/check-placeholders.ts`, `AGENTS.md`, сигнал через ntfy.sh. Двадцать
две ссылки на то, чего нет. Показательно, что и `app/README.md`, и
`app/CLAUDE.md` тратили по абзацу, объясняя читателю, что этому документу
верить нельзя: дисклеймер стоил дороже, чем документ давал. Уникального в нём
не осталось — границы слоёв держит `eslint.config.js` и описывает
`app/CLAUDE.md`, обоснование хранилища прогресса лежит комментарием в
`PreferencesProgressRepository.ts`, а ntfy заменён на PostHog ещё в сентябре.

**`docs/pipeline.md` (281 строка).** Появился как детальное описание конвейера
до того, как `docs/PLAY.md` стал каноническим, и после этого дублировал его.
Хуже: содержал таблицу из восьми слэш-команд — `/idea`, `/spec`, `/build` и
далее, — которых не существует. Папки `.claude/` в корне нет вовсе.
Аспирация, поданная как факт, — худший вид устаревшей документации: по ней
принимают решения. Дерево репозитория оттуда переехало в `CLAUDE.md`, где
«где что лежит» и должно быть; остальное было повторением.

Осталось три документа с тремя разными работами: **`docs/PLAY.md`** — что
такое конвейер, **`CLAUDE.md`** — правила и дерево, **`docs/decisions.md`** —
почему так вышло. Плюс README в папках для локальных деталей.

### Исправлено

**Дубль номера решения.** В `app/docs/decisions.md` было два разных `D-013`.
Второй стал `D-014`, ссылка в `app/CLAUDE.md` поправлена.

**Два журнала решений без указания областей.** Теперь каждый говорит в
заголовке, чей он: корневой — про проект (конвейер, ворота, пороги, спеки),
`app/docs/` — про фабрику (общий шелл, сборка). Решение, касающееся обоих,
пишется в корневой.

**Мёртвые поля в снимке доски.** `startDate`, `daysToStart`,
`dayOfExperiment` считались и уезжали в `status.json`, но страница их больше
не читает — дату она считает сама, а общей даты старта на доске нет. Убраны.

### Приведено к одной логике

**`distribution/posts-draft.md` был ловушкой.** Замороженный снимок текстов,
997 строк, подписанный «для ручного редактирования» — но генератор CSV его не
читал, и правки в нём молча никуда не шли. Хуже пустоты: файл выглядел рабочим.
Теперь он генерируется вместе с CSV и подписан как превью, с перечнем
настоящих источников. Соврать не может.

**Три генератора — одна проверка.** Выход всех трёх закоммичен: страницы хаба,
CSV с превью и снимок доски. Проверку на рассинхрон в CI имели только два.
Теперь проверка одна на всех — иначе один из трёх рано или поздно выпадет, и
это будет тот, про который забыли.

### Что проверено и оставлено

- `app/.claude/skills/phaser-*` — 12 000 строк, но они в `.gitignore` и
  синхронизируются из `node_modules/phaser` на `postinstall`. В репозитории
  их нет.
- `render-kit`: пять эффектов из шести не имеют потребителей. Это не мёртвый
  код, а общая библиотека для игр, которых ещё нет (D-012). Оставлено.
- `MemoryProgressRepository` — используется тестами шелла.
- `project.scheduleNote` в `games.json` — не читается скриптами, и это
  нормально: JSON не умеет комментариев, а объяснение провизорного порядка
  должно лежать там, где его увидят, открыв файл.

---

## 2026-09-21 — Шаг BUILD разделён: срез из одного уровня, потом пять

**Решение:** сборка игры больше не один шаг, а два прохода с воротами между
ними.

- **Проход 1 — срез.** Движок, рендер, HUD, тесты — всё настоящее, но
  `levels.json` из одного уровня и `levelCount: 1`. `npm run check` зелёный,
  на этом проход заканчивается.
- **GATE 4a.** Человек открывает срез на телефоне, три минуты, три ответа:
  идём / доработать / убить. Пишется полем `slice` в шапке
  `app/games/<slug>/review.md`.
- **Проход 2 — уровни.** Только после `slice: approved`: уровни 2–5, кривая
  сложности, kill-критерий на контенте, `levelCount: 5`.
- **GATE 4b.** Прежняя ручная приёмка, поле `release` в той же шапке.
  `release.mjs` держит публикацию именно по нему.

**Почему.** Ворота человека должны стоять там, где ответ «не то» ещё дёшев.
До этого решения человек впервые играл после пяти уровней — то есть когда
сделано уже всё: движок, рендер, HUD, тесты и подбор пяти раскладок. Убить
концепт в этой точке стоит дороже, чем всё, что можно было сэкономить.
Срез задаёт тот же вопрос на заходе раньше.

**Почему не одни ворота вместо двух.** Вопросы разные. На срезе — «механика
живая?». На готовой игре — «растёт ли сложность к пятому уровню и не стыдно
ли показать». Кривую через пять уровней чувствует человек: тест доказывает,
что бездумная игра перестаёт проходить с третьего уровня, но не скажет, что
четвёртый бесит.

**Цена.** Один лишний круг ожидания на каждую игру, тридцать за месяц, и
узкое место здесь человек. Поэтому проверка намеренно короткая и должна
открываться ссылкой на телефоне: проверка, которую неудобно сделать,
откладывается, а отложенные ворота — это ворота, которых нет.

**Как стадия считается.** Срез отличается от готовой игры числом уровней в
`levels.json` — отдельного поля «это срез» нет. Поле разошлось бы с
содержимым пака, а стадия обязана считаться из файлов (правило №1).

## 2026-09-21 — §7.1: картинка экрана в спеке

**Решение:** в шаблон спеки добавлен подраздел 7.1 «Экран — как выглядит
поле»: картинка или скетч (`specs/screens/NN-slug.png`) плюс словами — что
занимает экран сверху вниз, что читается за секунду, где происходит payoff.
Если механика построена на цвете — там же назван второй канал (форма,
значок, подпись). Валидатор предупреждает, если спека принята, а §7.1 нет,
и падает, если картинка в §7.1 ссылается на несуществующий файл.

**Почему.** Первая собранная по конвейеру игра (`box-arrives`) дала ровно
две ошибки, и обе — в раскладке, не в правилах. Поле схлопнулось в четыре
пикселя (процентная высота внутри `.game-surface` считается от auto-высоты),
а после починки канвас вылез за свою коробку (Phaser меряет родителя один
раз на старте, когда док ещё пустой). Правила были описаны так подробно, что
движок написался с первого раза и ни одно правило не пришлось выдумывать;
про экран в спеке не было сказано ничего — и его придумывали на ходу.

Значок на винте в той же игре пришлось добавить как «отступление от §7»:
механика целиком построена на цвете, и без второго канала игрок с
дальтонизмом не играет вовсе. Решать это во время сборки поздно — значок
меняет и раскладку, и размер элемента. Теперь это вопрос спеки.

**Нумерация.** Подраздел, а не тринадцатый раздел: на номера разделов
ссылается `app/CLAUDE.md` и комментарии в коде игр, а тридцать уже
написанных спек не должны переезжать ради новой строки.

---

## 2026-09-21 — Лаборатория: прототип открывается ссылкой с телефона

**Решение:** отдельный проект Vercel на этом же репозитории с Root Directory
`app`. Каждый пуш даёт собранные игры по адресу `<labUrl>/games/<slug>/`.
Адрес лежит в `games.json` → `project.labUrl`, доска подставляет его в пункты
Gate 4a и Gate 4b. Ссылка публичная: без логина, с любого телефона.
`app/public/robots.txt` закрывает её от индексации.

**Почему не хаб.** `g/<slug>/` — замороженные сборки: после публикации игра не
меняется (§9.1). Лаборатория ровно наоборот — всегда последняя. Класть обе
роли в один проект значит однажды подменить опубликованную игру.

**Почему не dev-сервер.** `npm run dev -- --host` работает, но требует ноут в
той же сети. Проверка Gate 4a рассчитана на три минуты; всё, что требует
сначала сесть за ноутбук, откладывается, а отложенные ворота — это ворота,
которых нет.

**Почему пустая строка — не ссылка по умолчанию.** Проект создаётся руками, и
пока `labUrl` пуст, доска зовёт поднимать dev-сервер и говорит, чего не
хватает. Ссылка, которой не существует, хуже её отсутствия — это тот же урок,
что и со сборкой снимка доски на деплое: конфигурация, которую нельзя
проверить, — не решение, а обещание.

## 2026-09-21 — События уходят только с домена хаба

**Решение:** `src/shell/reporting.ts`. `SignalSink` и `FeedbackSink` живые
только когда `location.hostname` — домен хаба. Везде иначе (dev-сервер,
Playwright, лаборатория, любой превью-деплой) шелл подставляет `Noop`-сборки.
`?signals=on` включает отправку где угодно, `?signals=off` выключает её на
хабе.

**Почему.** Каждая сборка каждой игры несёт один и тот же PostHog-токен —
публичный по устройству. До этого правила прохождение с dev-сервера, из e2e
или с превью попадало в ту же воронку, по которой выносится вердикт. Одно
тестовое прохождение — шум; тридцать прототипов тестовых прохождений — уже
неверный PROMOTE. Лаборатория, в которой человек играет в каждый срез, эту
проблему увеличивала бы в разы.

**Почему по адресу, а не по переменной окружения.** Переменную надо выставить
правильно на каждом хосте, где когда-либо окажется этот бандл, и забывается
она ровно там, где портит данные. Домен хаба — единственное место, где перед
игрой сидит настоящий игрок; это свойство адреса, а не сборки.

**Почему у флага есть обе стороны.** `?signals=on` нужен e2e: набор
утверждает контракт аналитики по настоящим сетевым вызовам. Публичность флага
безобидна — им можно добавить только свои события, но нельзя заглушить чужие.
`?signals=off` — обратный случай: показать игру кому-то с телефона, не
засчитав это прохождение.

**Проверено:** `tests/shell/reporting.test.ts` — пять случаев, включая хост,
который лишь содержит домен хаба; и e2e «without ?signals=on nothing reaches
the funnel» — настоящий прогон уровня, после которого перехватчик сетевых
вызовов пуст.

**Дополнено в тот же день (ревью системы):** та же дыра была у самого хаба.
`hub.js` шлёт `hub_impression` и `hub_click` — это верх той же воронки, и с
локального сервера они завышали «открытия», то есть занижали долю дошедших:
ровно ту цифру, по которой выносится вердикт. Починена половина — значит не
починено. Теперь тот же признак и та же пара флагов в `hub.js`, проверено
пятью случаями в `test-hub.mjs` и живым прогоном страницы (с локального
адреса — ноль запросов в PostHog, с `?signals=on` — запрос уходит).

Строка домена написана в двух местах намеренно: хаб и фабрика — две
независимые сборки без общего модуля, и общий модуль ради одной строки стоил
бы дороже. Каждое место называет второе; переезд домена правит оба.

---

## 2026-09-21 — box-arrives закрыт на Gate 4b; у «убить» появилось куда записываться

**Что произошло:** автор сыграл первую собранную игру конвейера и закрыл
концепт. Первый настоящий kill в проекте — и он сразу нашёл дыру в
позавчерашнем решении.

**Дыра.** Шаблон приёмки утверждал: «`rejected` конвейер не знает намеренно,
концепт убивается возвратом идеи в `ideas/rejected/`». На первом же случае
это не сработало дважды. Спеки 1–35 писались до появления папки идей — у
`box-arrives` файла идеи нет, возвращать нечего. И решение приняли на Gate
4b, а отправить его предлагалось туда, где стоит Gate 1. **Решение
записывается там, где принято** — иначе ворота принимают ответ, которому
негде лечь.

**Решение:**

- `rejected` — полноценное третье значение обоих полей `review.md`, рядом с
  `approved` и `rework`. `release.mjs` отказывается публиковать закрытый
  концепт отдельной проверкой, раньше проверки расписания: у закрытого
  концепта слаг с расписания уже снят, и без этого скрипт ругался бы
  правдиво, но не про то.
- `verdict: REJECTED` в `results/NN-slug.md` — чем концепт кончился, без
  цифр, с `killedAt: gate4a | gate4b`. Отдельный вердикт, а не `KILL`:
  `KILL` выносят игроки по воронке, `REJECTED` выносит автор на воротах, и в
  итогах тридцатки это две разные истории. Без такой записи в итогах было бы
  двадцать девять историй и молчаливая дыра.
- День в `games.json` освобождается: слаг снят, `status` обратно `planned`.

**Пустой день — отдельная строка доски.** Строки доски строятся по слагам,
поэтому день без слага с неё просто исчезал: не «день 1 ждёт концепт», а
вообще ничего. Это худший вид пустой клетки — его не видно даже как вопрос.
Теперь день без слага попадает в таблицу стадией «день пуст» и в «ждёт тебя»
пунктом «выбрать концепт на день», с адресом.

**Код игры остаётся в репозитории** и остаётся зелёным в CI. Он
доказательство, что концепт доводили до играбельного состояния, а не бросили
на бумаге, и единственный способ через месяц вспомнить, что именно не
сработало. Цена — лишние ~25 секунд e2e на прогон; если убитых игр станет
много, вернёмся к вопросу.

**Причина закрытия на момент записи отсутствует.** Файл приёмки говорит об
этом прямо, а доска держит пункт «закрыть концепт». Убитый концепт стоит
ровно столько, сколько из него вынесли: без строки «что именно не сработало»
у тридцатки остаётся факт «первый концепт убит» и ни одного урока.

---

## 2026-09-21 — Упрощение: двое ворот, дистрибуция на паузе, документы вдвое

**Повод.** Посчитали объём: машинерии ~2 500 строк, документов ~6 200, кода
игр ~1 400, опубликовано ноль. Система вшестеро больше того, ради чего
существует. Плюс сто двадцать решений человека за месяц при пяти воротах на
тридцать игр — при том, что узкое место здесь человек.

**Решение — резать церемонию, а не проверки.**

- **Ворота: пять → двое.** Спека принята и срез сыгран. Поле `slice` и поле
  `release` в приёмке игры схлопнуты в одно `review`; `release.mjs` держит
  публикацию по нему. Второй подписи под готовой игрой нет: запуск скрипта и
  есть решение публиковать. Минус тридцать кругов ожидания за месяц.
- **Дистрибуция заморожена** до первой публикации. `publer-posts.csv` и
  `posts-draft.md` больше не коммитятся и не проверяются в CI: расписание на
  тридцать дней вперёд для игр, которых нет, это не расписание, а фантазия
  (порядок дней провизорный, тизеры написаны до сборки). Скрипт и тексты на
  месте; первая вышедшая игра — повод разморозить и посмотреть, что из этого
  нужно на реальных данных.
- **Документы.** `docs/PLAY.md` 680 → 292 строк, `specs/HOWTO.md` (137)
  влит в `specs/_TEMPLATE.md` сжатой главой и удалён, шаблон приёмки
  145 → 87. Длинные обоснования не потеряны — они и так живут здесь, в
  журнале; из канона убраны повторы.

**Что не тронули:** доску (терминал и страницу), валидаторы, CI, тесты,
проверку по домену. Это проверки, а не церемония: они ловят то, что уже
ломалось.

**Правило №4 в `CLAUDE.md`: машинерия после второго случая.** Новое поле,
ворота, раздел или скрипт заводятся, когда дважды сломалось без них.
Основание — три сегодняшних примера: правило про «убить» написали
умозрительно и первый же kill его сломал; дистрибуция нагенерировала 533
строки постов для несуществующих игр; шесть тысяч строк прозы при нуле
опубликованных игр. Практический вывод: пока не вышли три игры, конвейер не
меняется, а наблюдения копятся здесь.

**Чего это стоит.** Часть знания, которое было записано «на будущее», теперь
не записано. Это принято сознательно: ненужное правило стоит дороже, чем
ненаписанное, — его читают, им руководствуются и его сопровождают.

---

## 2026-09-21 — Gate 1 переписан: шесть новых критериев и три условия порога

**Решение (формулировка автора, принята дословно).** Критерии:
`readability`, `motivation`, `decision_pressure`, `payoff`,
`differentiation`, `prototypeability` — по единой шкале 1–5, где 3 значит
«достаточно, чтобы проверить прототипом», а 5 — «центральное преимущество
игры». Порог: сумма ≥ 24 из 30, **каждый** критерий ≥ 3,
`prototypeability` ≥ 4, `kill_criterion` записан. Полосы решения: 26–30
приоритетный прототип, 24–25 обычный, 20–23 переработать или отложить,
ниже 20 закрыть.

Правило живёт в `ideas/README.md` — в той папке, где лежат идеи, и больше
нигде: шаблон и `docs/PLAY.md` на него ссылаются, а не повторяют.

**Что изменилось по сути, а не в названиях.** Ушли `pressure` (давление как
таковое) и `market` (попадание в тренд), пришли `decision_pressure` (есть ли
настоящий выбор, а не один очевидный ход) и `differentiation` (механическое
отличие от аналогов). Это сдвиг от «интересно ли звучит» к «есть ли что
проверять». `producibility` стал `prototypeability` и получил жёсткое
условие ≥ 4: конвейер однодневный, и идея, которую нельзя проверить за
вечер, отнимает не один день, а несколько прототипов.

**Первая жертва — единственная одобренная идея.** `arrow-flip` пересчитан:
25 из 30 (сумма взята), но `prototypeability` 3 — `authored` уровни плюс
обязательный солвер за вечер не укладываются, и это было написано в её же
обосновании ещё 20 сентября. `gate1` вернулся в `pending`. Изменилось не
качество идеи, а планка.

**Две проверки понижены с ошибки до предупреждения:** «спека есть, а идея не
одобрена» в обоих валидаторах. Причина — ровно этот случай: планка Gate 1
может смениться под уже написанной спекой, и это вопрос очереди к человеку,
а не повод ронять сборку всему репозиторию. `rejected` по-прежнему ошибка:
отклонённая идея со спекой — это противоречие, а не очередь.

**Концепты 1–35 не пересчитываются.** Они оценены по прежним критериям, и
`differentiation` тогда не оценивался вовсе — проставить его задним числом
значит выдумать число, а не измерить.

---

## 2026-09-21 — Gate 2: прототип → производство, по живым игрокам

**Решение (формулировка автора, принята дословно).** Вторые и последние
ворота концепта: собранный прототип проверяется на **10–15 живых игроках**.
Баллы Gate 1 здесь не значат ничего — предсказание уже проверено прототипом.

Четыре доли поведения: начали без объяснений ≥ 80%, поняли цель ≥ 70%,
продолжили добровольно ≥ 60%, повторили после поражения ≥ 50%. Четыре условия
«да/нет»: решения игроков разные, жадная стратегия не доминирует, пять уровней
ощущаются разными, стоимость производства в лимите. Результат —
`ready_for_production`, `rework` или `kill`.

Правило — `ideas/gate2.md`, рядом с правилом Gate 1: оба решают судьбу
концепта, и лежат в одной папке.

**Что это сломало и как разведено.** «Gate 2» уже был занят приёмкой спеки, а
проверка среза называлась воротами. Теперь ворот ровно двое — Gate 1 и
Gate 2, — а приёмка спеки, проверка среза и CI называются проверками: они
решают «доделывать ли», а не «жить ли концепту». Переименовано в `CLAUDE.md`,
`docs/PLAY.md`, шаблоне приёмки и в тексте доски.

**Вердикт по воронке заменён.** Прежнее правило (30+ прохождений, доля ≥ 25%
→ PROMOTE, ≤ 10% → KILL, иначе INCONCLUSIVE) удалено из `results/README.md`:
оно решало тот же вопрос по другим цифрам, а два правила для одного решения —
это отсутствие правила. Воронка осталась источником трёх долей из четырёх;
`gate2` заменил `verdict` в шапке результата и в `pipeline.mjs`.

**Чего воронка не умеет.** «Понимают цель» из неё не берётся никогда: события
показывают, что игрок что-то сделал, но не то, что он понял зачем. Под это
условие нужно посадить человека и спросить — и это записано прямо, чтобы
через месяц не подменить наблюдение удобной метрикой.

**Рейтинг выведен из решения.** Оценку ставят дошедшие до конца, то есть те,
кому уже понравилось; отвалившиеся уходят молча. Средняя по дошедшим измеряет
терпение выборки, а не игру.

**Порог 10 игроков — не формальность.** На восьми наблюдениях доля скачет на
десятки процентов; «ворота не пройдены» при меньшей выборке — честный статус,
а не «пока рано».

---

## 2026-09-21 — Шаблон спеки приведён к внешнему стандарту

**Повод.** Автор принёс отдельное исследование мировой практики
(Raw Fury, Supercell, MDA, ISO/IEC/IEEE 29148, RFC 2119, Gherkin, Rovio) и
рекомендуемую структуру спецификации прототипа. Шаблон переписан по ней.

**Что изменилось по существу, а не в нумерации.**

- **Одна гипотеза на прототип.** Поле `hypothesis` в паспорте и раздел 1
  «Граница прототипа»: что проверяем, какой минимальный цикл для этого нужен,
  что сознательно не делаем, сколько уровней нужно для честной проверки. Всё,
  что не помогает проверить гипотезу, из прототипа исключается.
- **Каждый исход действия — отдельная строка таблицы** (действие, условие,
  изменение состояния, видимый результат, потрачен ли ход). Прежний раздел
  «Правила» допускал прозу со скрытым продолжением.
- **Раздел «Объекты и состояние»** с таблицей полей и типом `GameState` —
  словарь, без которого правила нельзя записать точно.
- **Порядок автоматических проверок после хода** назван явно. Раньше его не
  требовал никто, и он всплывал во время сборки.
- **План проверки записан до сборки** (раздел 8): доли, обязательные условия
  и kill. Метрики отвечают на гипотезу, а не собираются «на будущее».
- **Профиль фабрики вынесен в приложение A.** Разделы 1–8 описывают игру и не
  зависят от того, на чём она собрана; сменится платформа — перепишется
  приложение.

**Тринадцать разделов стали девятью.** Ушли отдельные «Паспорт концепта»
(дублировал шапку), «Метрики и события» (профиль фабрики) и «Крайние случаи»
(теперь это требование к разделу 4: для каждого ввода валидный, невалидный и
пограничный исход). Разделы 9.1–9.3 разошлись: что относится к правилам игры
— в разделы 1 и 5, что к платформе — в приложение.

**Убрано всё про 30/30.** В шаблоне не осталось ни «эксперимента», ни
«тридцати игр», ни «месяца измерения»: документ описывает спецификацию одной
игры и больше ничего. Ограничения, которые были обоснованы сравнимостью
тридцати прототипов, переформулированы по существу: случайность запрещена
потому, что иначе две попытки одного игрока — это две разные игры.

**`review` → `spec_status: draft | review | approved`.** Принятие спеки
перестало называться Gate 2 ещё утром; теперь и поле называется состоянием
документа. Мигрированы все 31 спека, поле `reviewed` стало `spec_reviewed`.

**Что валидатор теперь ловит:** `approved` без гипотезы, `approved` с
оставшимся в тексте `OPEN`, `approved` без даты. Тридцать спек без
`hypothesis` печатаются **одной строкой**, а не тридцатью: тридцать
одинаковых предупреждений утопят те два, что про конкретную спеку.

**Чего не делали.** Тридцать написанных спек не переписывались под новую
структуру. Они прошли прежний шаблон, их разделы на месте, а переписывание
9 500 строк ради нумерации — это работа без результата. Новая структура
действует для новых спек; старая доживает до своей сборки.

---

## 2026-09-21 — Шаблоны и правила ворот переезжают к своим артефактам

**Решение:** два переезда, одно правило раскладки.

1. `docs/templates/` расформирована. Шаблон идеи → `ideas/_TEMPLATE.md`,
   шаблон проверки среза → `app/games/_REVIEW.md`, шаблон медиа →
   `app/games/_MEDIA.md`.
2. Правило Gate 2 → `results/gate2.md` (было `ideas/gate2.md`).

**Правило:** документ лежит там, где лежит то, что по нему пишут. Шаблон —
рядом с файлами, которые по нему заполняются. Правило ворот — рядом с файлом,
куда записывается решение.

**Почему.** Шаблонов было три, и лежали они по трём разным конвенциям:
шаблон спеки — в `specs/` рядом со спеками, а шаблон идеи и шаблон приёмки —
в общей папке за два поворота от своих артефактов. Правильна одна из трёх, и
это `specs/_TEMPLATE.md`: `ideas/README.md` вынужден был ссылаться наружу за
шаблоном, который описывает файлы в его же папке.

С Gate 2 то же самое, и это ошибка от 2026-09-21 (см. запись выше): правило
положили в `ideas/` с аргументом «чтобы два правила ворот не разошлись».
Аргумент оказался пустым — критерии у Gate 1 и Gate 2 разные, расходиться
нечему, — а `results/README.md` пришлось писать «правило в `../ideas/`».
Теперь `ideas/` — про идеи, `results/` — про итог, и ни одна папка не хранит
правило о чужой.

**Чего не делали.** Хаб (`index.html`, `styles.css`, `hub.js`, `404.html`)
остаётся в корне вперемешку со скриптами конвейера. Собрать его в `hub/`
хочется, но `index.html` в корне — это то, как его отдаёт хостинг: переезд
стоит правки деплоя ради красоты. По правилу №4 — нет, пока это не сломается
дважды.

**Побочно:** `release.mjs` перечисляет собранные игры через `withFileTypes` —
иначе шаблоны в `app/games/` попали бы в список игр.

---

## 2026-09-21 — Gate 1 становится файлом, README возвращается в указатели

**Решение:** правило первых ворот переехало из `ideas/README.md` в
`ideas/gate1.md`. `ideas/README.md` стал коротким указателем — как в
`trends/`, `specs/`, `results/`, `data/`.

**Почему.** После вчерашнего переезда Gate 2 в `results/gate2.md` вышла
асимметрия: вторые ворота — файл с говорящим именем, первые — спрятаны в
README. Проверилось живым способом: автор искал Gate 1 по репозиторию и не
нашёл, хотя правило писал сам неделю назад.

Плюс `ideas/README.md` работал в двух ролях сразу — указатель папки и текст
правила на сто строк с таблицами. У всех остальных папок README это три
абзаца «откуда пришло, куда дальше»; читающий репозиторий сверху вниз
натыкался на исключение ровно в той папке, с которой конвейер начинается.

Теперь единообразно: README указывает, правила ворот лежат файлами
`ideas/gate1.md` и `results/gate2.md`, каждое рядом со своим решением.

**Побочно:** в `trends/README.md` чинилась ссылка на критерий `market` — его
переименовали в `differentiation`, а текст остался. Протухла она вчера,
нашлась при обходе ссылок сегодня.

---

## 2026-09-21 — ideas/validate.mjs: family, twist и kill_criterion становятся обязательными

**Решение:** в `REQUIRED` добавлены `family`, `twist`, `kill_criterion`.
`kill_criterion` дополнительно перестал проверяться отдельной веткой внутри
блока Gate 1 — дубль убран, поле теперь ловится там же, где остальные
обязательные.

**Почему разошлось.** Три поля есть в шапке шаблона (`ideas/_TEMPLATE.md`) и
реально заполнены в единственной живой идее, но скрипт их не требовал:

- `family` не проверялся вовсе. `specs/validate.mjs` ждёт его как
  наследуемое поле и предупреждает при расхождении — то есть дыра в идее
  всплывала на шаг позже, у спеки, а не там, где её было проще исправить.
- `twist` — симметричная пара `pressure` («одно главное давление и один
  главный хук», `_TEMPLATE.md` §«Правила заполнения»). `pressure` был
  обязателен, `twist` — нет, без причины для разницы.
- `kill_criterion` по `ideas/gate1.md` обязателен и пишется в разделе 8 ещё
  до оценки, а в коде проверялся только внутри ветки, которая срабатывает
  лишь когда `score` уже заполнен. Идея без баллов и без kill_criterion
  проходила с одним предупреждением «не оценена».

Нашлось при сверке кода с `ideas/gate1.md` и `ideas/_TEMPLATE.md` — скрипт не
успевал за документами, которые сам же и проверяет.

**Проверено:** `ideas/active/36-arrow-flip.md` (все три поля уже заполнены)
проходит без новых ошибок; идея с искусственно убранным `family` — падает.

---

## 2026-09-21 — twist убран из REQUIRED: правило не пережило проверку тридцатью спеками

**Решение:** `twist` больше не обязателен в `ideas/validate.mjs`. Остаётся
обязателен только `pressure`.

**Как нашлось.** При подготовке задачи «перенести 30 спек в идеи» пришлось
пройтись по шапкам всех тридцати. У пятнадцати из тридцати — ровно половины —
`twist: []`, и это не недосмотр: часть этих концептов уже собрана и прошла
приёмку. Правило «обязателен один хук вдобавок к давлению», которое я час
назад вывел из формулировки `_TEMPLATE.md` («один главный хук») и из
единственного живого примера (arrow-flip, где twist заполнен), не пережило
проверку тридцатью реальными концептами.

**Урок:** решение по одному примеру и по формулировке правила — не то же
самое, что решение по корпусу данных. Тридцать спек были под рукой всё это
время; стоило сверить с ними до того, как менять валидатор, а не после.

`family` и `kill_criterion` тем же способом сверены по всем тридцати — оба
поля заполнены везде без исключений, эта часть правки остаётся.

---

## 2026-09-21 — 30 спек перенесены назад в идеи

**Решение:** для концептов 1–35 (все, у кого есть спека, кроме уже
существующей идеи arrow-flip) созданы `ideas/active/NN-slug.md` по
`ideas/_TEMPLATE.md`. Источник — шапка и разделы 2–3 соответствующей спеки:
эти концепты писались до появления `ideas/`, спека была единственным
документом.

**Что перенесено механически, без интерпретации:** `slug`, `number`,
`title_ru`, `title_en`, `verb`, `pressure`, `twist`, `emotion`, `family`,
`levels`, `solver`, `kill_criterion` — дословно из шапки спеки. Раздел 8
«Вопрос прототипа» тоже несёт kill_criterion дословно; гипотеза и критерий
успеха там, где явного `hypothesis` не было (29 из 30), сформулированы как
прямое прочтение kill_criterion — он самим своим существованием и есть
позитивное утверждение наоборот.

**Что сформулировано заново:** разделы 3 «Игровой цикл» и 4 «Главное
решение» — их нет в старом формате спеки как отдельного текста, только как
занумерованные правила движка. Составлено по правилам раздела 3 спеки и
условиям победы/поражения раздела 2, без домыслов сверх того, что там
написано.

**Score сознательно не перенесён и не пересчитан** — решение из прошлой
записи (`gate1.md`, «Концепты 1–35») здесь не пересматривалось. `gate1:
approved` проставлен по факту: концепт дошёл до спеки. `gate1_date` — дата
первого коммита спеки в git-истории, не выдуманная.

**Как нашлось несоответствие в валидаторе.** `approved` без `score` — до
этой правки была ошибка (`gate1=X без оценки`), рассчитанная на новый
конвейер, где approved без оценки — либо забыли посчитать, либо подгоняют
решение под цифры. Для тридцати концептов это не тот случай: score в
принципе не должен появляться, потому что differentiation тогда не
оценивался. Добавлено исключение `LEGACY_MAX_NUMBER = 35`, ровно граница,
уже задокументированная в `gate1.md`. Тридцать одинаковых предупреждений
собраны в одну строку — тем же приёмом, что и `withoutHypothesis` в
`specs/validate.mjs`.

**Проверено:** `specs/validate.mjs` — 0 ошибок, шапки идей и спек совпадают
по всем наследуемым полям (иначе INHERITED-проверка бы упала). `ideas/
validate.mjs` — 0 ошибок, 3 предупреждения (arrow-flip × 2, сводка по
тридцати). Спеки не редактировались.
