---
slug: box-arrives
number: 1
title_ru: Ящик приезжает
title_en: Incoming Box
pitch_ru: >-
  Откручивай винты под текущие ящики; каждые три хода ящик меняется,
  карман вмещает три.
pitch_en: >-
  Unscrew what matches the boxes below. Every three moves a box leaves,
  and the pocket holds three.
verb: извлечение
pressure:
  - навязанная очередь
twist: []
emotion: ритм — «я вовремя подготовился к следующему ящику»
family: screw
levels: generated
solver: none
score:
  readability: 4
  motivation: 5
  pressure: 4
  payoff: 4
  producibility: 5
  market: 5
kill_criterion: >-
  Если preview очереди ящиков не заставляет менять порядок откручивания
  минимум в ключевых ситуациях уровня 5, концепт хоронится.
review: pending
reviewed:
---
# 1. Паспорт концепта

Поля паспорта — в шапке раздела 0.

**Почему kill-критерий именно такой.** Твист здесь — видимая очередь ящиков. Если она не меняет порядок откручивания, значит preview — украшение, а игра сводится к обычному «откручивай подходящий цвет», каких на рынке уже много. Провал по критерию означает, что навязанная очередь не создаёт планирования: это свойство идеи, а не баланса, потому что при жадной игре preview остаётся мёртвым на любой раскладке винтов.

# 2. Цель и условия

Правила уровня — текст в кнопке «?»:

Откручивай винты. Внизу стоят два активных ящика. Каждые 3 хода левый уезжает, правый сдвигается влево, а справа приезжает следующий. Цвета следующих ящиков видны заранее. Если нужного цвета сейчас нет, винт идёт в карман на 3 места. Когда подходящий ящик приезжает, такие винты автоматически уходят из кармана.

- Условие ПОБЕДЫ: на поле не осталось винтов.
- Условие ПОРАЖЕНИЯ: игрок откручивает винт недоступного цвета, когда все 3 места кармана заняты. Попытка положить четвёртый винт = немедленный проигрыш.
- Runtime RNG нет. Порядок ящиков, цвета и расположение винтов заранее зафиксированы.

# 3. Правила

1. На старте загружаются винты, два активных ящика и фиксированная очередь следующих ящиков.
2. Счётчик смены начинается с `3`, карман пуст.
3. Игрок тапает по любому винту, который ещё находится на поле.
4. Валидный тап откручивает винт и считается одним ходом.
5. Если цвет винта совпадает с одним из активных ящиков, винт сразу улетает в него.
6. Если совпадения нет и в кармане есть место, винт занимает первый свободный слот.
7. Если совпадения нет и карман уже `3/3`, фиксируется поражение по п.2.
8. После успешного хода счётчик смены уменьшается на 1.
9. При `0` левый ящик уезжает, правый становится левым, первый из очереди приезжает справа, счётчик снова становится `3`.
10. После смены все винты из кармана, подходящие под любой активный ящик, автоматически уходят туда. Это не считается ходом.
11. Игрок не двигает ящики, не меняет очередь и не может пропустить ход.
12. Если после разрешения хода винтов на поле не осталось, фиксируется победа по п.2.

# 4. Состояние уровня

```ts
type GameState = {
  remainingScrews: string[];
  pocket: { screwId: string; color: string }[];
  activeBoxes: [string, string];
  boxQueue: string[];
  moves: number;
  movesUntilShift: 1 | 2 | 3;
  status: 'playing' | 'won' | 'failed';
};
```

# 5. Ввод

```ts
{ type: 'tap'; targetId: string }
```

Один игровой жест — тап по винту. Тап по HUD, ящикам или карману игровым действием не считается.

# 6. Контент уровня

Пример `levels.json`:

```json
{
  "id": 1,
  "shiftEvery": 3,
  "pocketSize": 3,
  "queuePreview": 3,
  "screws": [
    { "id": "s1", "color": "c1", "x": 0.24, "y": 0.31 }
  ],
  "boxes": ["c1", "c2", "c3", "c1", "c4", "c2"]
}
```

Первые два `boxes` активны, остальные образуют очередь. Генерация выполняется до публикации и сохраняется как фиксированный контент.

Кривая по пяти уровням:

1. 3 цвета, 10 винтов. Почти все винты подходят текущим ящикам; карман нужен один раз.
2. 3 цвета, 13 винтов. Нужно заметить смену через 3 хода и один раз подготовиться к следующему ящику.
3. 4 цвета, 16 винтов. Первый настоящий выбор между винтом доступного цвета и цветом, который скоро приедет.
4. 4 цвета, 20 винтов. Карман регулярно доходит до `2/3`; неправильный порядок создаёт угрозу переполнения.
5. 5 цветов, 24 винта. Минимум три ситуации, где preview очереди должен менять порядок откручивания.

# 7. Визуал

- Поле: одна плита с крупными круглыми винтами; никаких падающих деталей и дополнительной физики.
- Внизу: два крупных активных ящика; рядом три меньших preview-ящика.
- Карман: три постоянных слота `○ ○ ○`.
- Выбрано: винт приподнимается и делает короткий оборот.
- Выполнено: винт быстро летит в подходящий ящик.
- Карман: неподходящий винт летит в свободный слот.
- Payoff: при смене старый ящик уезжает, новый въезжает, подходящие винты каскадом вылетают из кармана. Событие должно читаться примерно за секунду.
- Ошибка: при четвёртом винте карман дёргается, винту некуда встать, сразу появляется fail.
- HUD: `Осталось: N`, `Смена через: 3/2/1`, `Карман: N/3`, preview очереди.

# 8. Онбординг

1. Нажми на винт, чтобы открутить его.
2. Подходящий цвет сразу уйдёт в один из двух ящиков.
3. Неподходящий винт попадёт в карман.
4. В кармане только 3 места.
5. Каждые 3 хода приезжает новый ящик.
6. Следующие ящики видны заранее.

Кнопка `?` показывает только правило из п.2.

# 9. Явно ВНЕ рамок

- Без меты, валют, коллекций, заданий, ежедневных бонусов, рекламы, IAP.
- Без runtime RNG.
- Максимум одно давление: навязанная очередь.
- Твист отсутствует.
- Без бустеров, undo, перестановки винтов, ручного управления ящиками, пропуска хода и вместимости ящиков.
- Без изменений баланса/уровней после публикации, кроме датированного критического бага.

# 10. Метрики и события

Общий shell логирует жизненный цикл уровня и аналитику. Механика не вызывает
PostHog напрямую.

Для этой игры механика сообщает shell:

- `onFirstAction()` — при первом валидном тапе по винту на поле. Shell
  отправляет `first_action`.
- `onFail('pocket_overflow')` — при попытке положить четвёртый неподходящий
  винт в заполненный карман. Shell отправляет `level_fail` и показывает
  fail-popup.
- `onComplete()` — когда на поле не осталось винтов. Shell отправляет
  `level_win`; для пятого уровня он сам добавляет `level_5_complete`.
- `retry` отправляет shell при нажатии «Ещё раз» после поражения.

Kill-критерий концепта: если preview очереди ящиков не заставляет менять порядок
откручивания минимум в ключевых ситуациях уровня 5, концепт хоронится.

Онлайн-резерв: читаемость, доходимость до 5 уровня, возврат D1. Если критерий не
сработал — концепт не чинится.

# 11. Крайние случаи

- **Повторный тап по тому же винту** — второй тап приходит по уже удалённому винту: цели нет, ничего не происходит, ход не засчитывается (правило 3).
- **Тап мимо всего** — тап по полю, HUD, ящикам и карману игровым действием не считается (§5) — состояние не меняется.
- **Действие невозможно** — недоступных винтов нет: тапнуть можно по любому оставшемуся. Позиция, где каждый оставшийся цвет недоступен, а карман 3/3, — это проигрышная позиция, и движок её не предотвращает: следующий тап даёт поражение по п.7.
- **Ходов не осталось, победа не выполнена** — неприменимо: лимита ходов нет, уровень ограничен только количеством винтов.
- **Два объекта в одной позиции** — неприменимо: винты занимают фиксированные точки поля и не двигаются, карман держит три отдельных слота.

# 12. Профиль реализации

Относится к фабрике, а не к игровой идее: сменится фабрика — перепишется
только этот раздел.

**Контракт.** Механика реализует `MechanicHost` из `src/shell-contract.ts` и
вызывает `onFirstAction()`, `onComplete()`, `onFail(reason)`, `onExit()`.
PostHog напрямую не трогает — см. раздел 10.

**`game.config.ts`.** `id` совпадает со `slug` из шапки, `title` — с
`title_ru`, `tagline` — короткая строка под названием, `levelCount: 5`,
`version` и `onboarding.version` поднимаются при изменении контента и правил
соответственно. Токены аналитики и ключ обратной связи копируются из
`games/tap-targets/game.config.ts`.

**`data-testid`.** Обязателен на каждом интерактивном элементе — e2e ищет
строго по ним. Существующая конвенция: `game-surface` — контейнер механики,
`mechanic-hud` — HUD с атрибутом `data-remaining`.

**Анимация.** Payoff строится из эффектов `render-kit`: `punch`,
`invalidShake`, `successFlash`, `fadeCollapse`, `cameraShake`, `moveTo`.
Если нужного нет — сказать здесь, а не изобретать свой во время сборки.
_Какие именно эффекты нужны этой игре — не записано._
